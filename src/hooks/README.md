# hooks/

Custom React hooks that encapsulate page logic and reusable behavior.

- **useLogEntry** — All Log Entry page state and actions (tasks, project, validation, submit, smart defaults)
- **useDashboard** — Dashboard filters, computed stats, chart data, and grouped history
- **useToast** — Toast notification state management (add, remove, auto-dismiss)
