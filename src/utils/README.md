# utils/

Shared utilities, type definitions, and constants.

- **types.ts** — TypeScript interfaces for Entry, Schema, Preferences, TaskFormState, etc.
- **helpers.ts** — Pure utility functions (date formatting, ID generation, CSV conversion, groupBy, countBy)
- **defaultSchema.ts** — Default schema data with all technologies, groups, and labels
