# services/

External service integrations and data persistence layer.

- **firebase.ts** — Firebase initialization and Firestore instance
- **storageService.ts** — Unified read/write API with Firebase primary and localStorage fallback
- **exportService.ts** — CSV data export and JSON schema export with browser download
