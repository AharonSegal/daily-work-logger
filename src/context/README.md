# context/

React Context providers for global state management.

- **AppContext** — Central state for entries, schema, preferences, and current page. Provides actions: addEntries, updateSchema, updatePreferences, clearData, setPage.
